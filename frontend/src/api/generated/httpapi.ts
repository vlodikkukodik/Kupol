// Code generated by tygo. DO NOT EDIT.
// Код сгенерирован tygo из backend/internal/httpapi (make types). Не править руками.
import type * as documents from "./documents";

//////////
// source: api_types.go

/**
 * SessionResponse — GET /api/auth/session. Для Гражданина (не вошёл) user = null: 401 на каждой загрузке страницы
 * засорял бы консоль браузера и журналы.
 */
export interface SessionResponse {
  user?: UserDTO | null;
}
/**
 * CaptchaResponse — GET /api/auth/captcha: вопрос анкеты для регистрации.
 */
export interface CaptchaResponse {
  id: string;
  question: string;
}
/**
 * LoginResponse — POST /api/auth/login.
 */
export interface LoginResponse {
  user: UserDTO;
  /**
   * LevelUp — этот вход поднял уровень (XP перешёл порог) — повод показать штамп «ДОПУСК ПОВЫШЕН».
   */
  level_up: boolean;
}
/**
 * RegisterResponse — POST /api/auth/register: пользователь и резервный код, который показывается ОДИН раз.
 */
export interface RegisterResponse {
  user: UserDTO;
  backup_code: string;
}
/**
 * RestoreResponse — POST /api/auth/restore: НОВЫЙ резервный код (показывается один раз) и, если человек вошёл, он сам.
 * User = null — у него включён код из приложения: пароль сменён, но входить нужно обычным путём, с кодом.
 */
export interface RestoreResponse {
  user?: UserDTO | null;
  backup_code: string;
}
/**
 * TOTPStatusResponse — GET /api/me/totp: состояние защиты кодом из приложения.
 */
export interface TOTPStatusResponse {
  enabled: boolean;
  /**
   * RecoveryLeft — сколько одноразовых кодов ещё не потрачено (0, если защита выключена).
   */
  recovery_left: number /* int */;
}
/**
 * TOTPSetupResponse — POST /api/me/totp/setup: что показать при подключении. Secret — для ручного ввода в приложение,
 * URI — ссылка otpauth://, которую кодирует QR. Защита ещё не включена: её включает первый верный код (POST …/enable).
 */
export interface TOTPSetupResponse {
  secret: string;
  uri: string;
}
/**
 * TOTPCodesResponse — одноразовые коды на случай потери телефона; показываются ОДИН раз.
 */
export interface TOTPCodesResponse {
  recovery_codes: string[];
}
/**
 * RecentResponse — GET /api/documents/recent: лента «Поступило в ЦАК».
 */
export interface RecentResponse {
  items: documents.Item[];
}
/**
 * DocumentResponse — GET /api/documents/:ref: документ, собранный заново по допуску читателя.
 */
export interface DocumentResponse {
  document: documents.OutDocument;
}
/**
 * DirectorateInfoDTO — что подразумевает Директорат (для страницы «Роли и права»).
 */
export interface DirectorateInfoDTO {
  name: string;
  capabilities: CapabilityDTO[];
}
/**
 * TeamRolesResponse — GET /api/team/roles: какие бывают роли и что каждая позволяет.
 */
export interface TeamRolesResponse {
  roles: RoleInfoDTO[];
  directorate: DirectorateInfoDTO;
  capabilities: CapabilityDTO[];
}
/**
 * TeamMembersResponse — GET /api/team/members: страница пользователей с ролями.
 */
export interface TeamMembersResponse {
  members: MemberDTO[];
  total: number /* int64 */;
  page: number /* int */;
  per_page: number /* int */;
  pages: number /* int */;
}
/**
 * MemberRoleResponse — PUT/DELETE /api/team/members/:login/roles/:role. changed=false — роль уже была (или её не было).
 */
export interface MemberRoleResponse {
  member: MemberDTO;
  changed: boolean;
}
/**
 * TeamDocumentResponse — создание и чтение документа в team panel.
 */
export interface TeamDocumentResponse {
  document: documents.TeamDocument;
}
/**
 * LockResponse — POST /api/team/documents/:id/lock.
 */
export interface LockResponse {
  lock: documents.LockInfo;
}
/**
 * VersionResponse — GET /api/team/documents/:id/versions/:vid.
 */
export interface VersionResponse {
  version: documents.VersionFull;
}
/**
 * DiffResponse — GET /api/team/documents/:id/versions/:vid/diff.
 */
export interface DiffResponse {
  diff: documents.Diff;
}
/**
 * ReviewResponse — GET /api/team/documents/:id/review: ход рецензии и комментарии.
 */
export interface ReviewResponse {
  review: any /* documents.ReviewInfo */;
}
/**
 * LintResponse — GET /api/team/documents/:id/lint.
 */
export interface LintResponse {
  lint: any /* documents.LintReport */;
}
/**
 * CommentResponse — комментарий рецензента.
 */
export interface CommentResponse {
  comment: any /* documents.CommentOut */;
}
/**
 * SubmitRequest — POST /api/team/documents/:id/submit. BaseRevision — редакция, которую видел автор.
 */
export interface SubmitRequest {
  base_revision: number /* int */;
}
/**
 * NoteRequest — тело действий с необязательным пояснением (забрать с проверки, в архив, из архива).
 */
export interface NoteRequest {
  comment: string;
}
/**
 * VerdictRequest — POST /api/team/documents/:id/verdict. BaseRevision — редакция, которую читал рецензент.
 */
export interface VerdictRequest {
  verdict: 'approve' | 'return' | 'reject';
  comment: string;
  base_revision: number /* int */;
}
/**
 * AddCommentRequest — POST /api/team/documents/:id/comments. Без block_id комментарий относится к документу целиком.
 */
export interface AddCommentRequest {
  block_id?: string;
  body: string;
}
/**
 * ResolveCommentRequest — PUT /api/team/documents/:id/comments/:cid.
 */
export interface ResolveCommentRequest {
  resolved: boolean;
}
/**
 * SiteResponse — GET /api/site: публичные настройки сайта (контакты автора для страницы «О КУПОЛЕ»).
 */
export interface SiteResponse {
  site: any /* documents.SiteSettings */;
}
/**
 * SiteSettingsResponse — GET/PUT /api/team/site: настройки для панели команды.
 */
export interface SiteSettingsResponse {
  site: any /* documents.SiteSettingsOut */;
}
/**
 * TimelineResponse — GET /api/timeline: хронология «О КУПОЛЕ» для читателя.
 */
export interface TimelineResponse {
  items: any /* documents.TimelineItem */[];
}
/**
 * TimelineEventsResponse — GET /api/team/timeline: все события для редактирования.
 */
export interface TimelineEventsResponse {
  items: any /* documents.TimelineEventOut */[];
}
/**
 * TimelineEventResponse — событие хронологии.
 */
export interface TimelineEventResponse {
  event: any /* documents.TimelineEventOut */;
}
/**
 * DashboardResponse — GET /api/team/dashboard: рабочий стол человека.
 */
export interface DashboardResponse {
  dashboard: any /* documents.Dashboard */;
}
/**
 * TemplatesResponse — GET /api/team/templates.
 */
export interface TemplatesResponse {
  items: any /* documents.TemplateItem */[];
}
/**
 * TemplateResponse — шаблон целиком (чтение, создание, правка).
 */
export interface TemplateResponse {
  template: any /* documents.TemplateFull */;
}
/**
 * GlossaryResponse — GET /api/team/glossary.
 */
export interface GlossaryResponse {
  items: any /* documents.TermOut */[];
}
/**
 * TermResponse — термин глоссария (создание, правка).
 */
export interface TermResponse {
  term: any /* documents.TermOut */;
}
/**
 * UpdateTemplateRequest — PUT /api/team/templates/:id. Без content меняются только название и описание.
 */
export interface UpdateTemplateRequest {
  name: string;
  description: string;
  content?: any /* documents.TemplateContent */;
}

//////////
// source: auth.go

export interface RoleDTO {
  id: string;
  name: string;
}
export interface UserDTO {
  login: string;
  level: number /* int */;
  level_name: string;
  directorate: boolean;
  created_at: string /* RFC 3339 */;
  /**
   * Roles — роли команды; Capabilities — что пользователю разрешено (готовый список: интерфейс
   * сам права из ролей не выводит, решает сервер).
   */
  roles: RoleDTO[];
  capabilities: string[];
  /**
   * TOTPEnabled — включён ли вход с кодом из приложения.
   */
  totp_enabled: boolean;
  /**
   * XP, LoginStreak — очки опыта и серия ежедневных входов подряд (шаг 5.1).
   */
  xp: number /* int */;
  login_streak: number /* int */;
  /**
   * NextLevelXP — сколько XP нужно для следующего уровня; 0 — дальше только решением Особого Совета.
   */
  next_level_xp: number /* int */;
  /**
   * Email — подтверждённая почта (шаг 5.1.1, по желанию); PendingEmail — указана, но ссылка ещё не открыта.
   */
  email?: string;
  pending_email?: string;
}
export interface RegisterRequest {
  login: string;
  password: string;
  captcha_id: string;
  captcha_answer: string;
}
export interface LoginRequest {
  login: string;
  password: string;
  /**
   * TOTP — код из приложения или одноразовый код; нужен, только если у пользователя включена защита кодом
   * (без него сервер отвечает 401 totp_required — после проверки пароля).
   */
  totp?: string;
}
export interface RestoreRequest {
  login: string;
  backup_code: string;
  new_password: string;
}
export interface ChangePasswordRequest {
  current_password: string;
  new_password: string;
}
export interface DeleteAccountRequest {
  password: string;
}
export interface SetEmailRequest {
  password: string;
  email: string;
}
export interface ConfirmEmailRequest {
  token: string;
}

//////////
// source: documents.go


//////////
// source: errors.go

/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeNotFound = "not_found";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeMethodNotAllowed = "method_not_allowed";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeInternal = "internal";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeBadProxySig = "bad_proxy_signature";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeTooLarge = "payload_too_large";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeBadRequest = "bad_request";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeUnsupportedMedia = "unsupported_media_type";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeForbiddenOrigin = "forbidden_origin";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeUnauthenticated = "unauthenticated";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeInvalidCredentials = "invalid_credentials";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeWrongPassword = "wrong_password";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeValidation = "validation";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeLoginTaken = "login_taken";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeCaptchaFailed = "captcha_failed";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeRateLimited = "rate_limited";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeAccessDenied = "access_denied";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeForbidden = "forbidden"; // вошёл, но для действия нужна роль или право
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeLocked = "locked"; // документ правит другой человек
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeConflict = "conflict"; // документ изменён после того, как его открыл редактор
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeCodeTaken = "code_taken";
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeNameTaken = "name_taken"; // название шаблона уже занято
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeSelfReview = "self_review"; // Редактор пытается проверить собственный документ
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeInvalidState = "invalid_state"; // действие не подходит документу в его статусе
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeLintFailed = "lint_failed"; // документ не прошёл проверку канона; отчёт — в lint
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeTOTPRequired = "totp_required"; // пароль верен, но нужен код из приложения
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeTOTPInvalid = "totp_invalid"; // неверный, просроченный или уже использованный код
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeTOTPAlreadyEnabled = "totp_already_enabled"; // код из приложения уже включён
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeTOTPNotEnabled = "totp_not_enabled"; // код из приложения не включён (или подключение не начато)
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeEmailTaken = "email_taken"; // почта уже подтверждена другим аккаунтом
/**
 * Коды ошибок API. Фронтенд ориентируется на code, а не на текст.
 */
export const CodeEmailTokenInvalid = "email_token_invalid"; // ссылка подтверждения недействительна, устарела или уже использована
export interface ErrorBody {
  error: ErrorDetail;
}
export interface ErrorDetail {
  code: string;
  message: string;
  request_id?: string;
  /**
   * Fields — ошибки по полям формы (ключ — имя поля в JSON запроса).
   */
  fields?: { [key: string]: string};
  /**
   * RetryAfter — через сколько секунд можно повторить (для 429).
   */
  retry_after?: number /* int */;
  /**
   * RequiredLevel и RequiredLevelName — какой допуск нужен (для 403 «Доступ запрещён»).
   */
  required_level?: number /* int */;
  required_level_name?: string;
  /**
   * Problems — замечания к содержимому документа (путь в JSON и текст) при ответе 422.
   */
  problems?: documents.Problem[];
  /**
   * Lock — кто правит документ (409 locked); CurrentRevision — актуальная редакция (409 conflict).
   */
  lock?: LockDetail;
  current_revision?: number /* int */;
  /**
   * Lint — отчёт линтера канона (422 lint_failed): что мешает отправить или опубликовать документ.
   */
  lint?: any /* documents.LintReport */;
}
export interface LockDetail {
  holder: string;
  expires_at: string /* RFC 3339 */;
}

//////////
// source: guards.go


//////////
// source: health.go

export interface HealthResponse {
  status: string;
  db: string;
  version: string;
  client_ip: string;
  ip_source: string;
}

//////////
// source: middleware.go

export const HeaderRequestID = "X-Request-Id";
/**
 * MaxBodyBytes — лимит тела обычных JSON-запросов. Загрузки файлов идут
 * отдельным маршрутом с собственным лимитом.
 */
export const MaxBodyBytes = 1 << 20;
export const IPSourceProxy = "proxy"; // IP подтверждён подписью PHP-прокси
export const IPSourceDirect = "direct"; // IP из TCP-соединения / доверенного обратного прокси

//////////
// source: router.go
/*
Package httpapi собирает HTTP-маршрутизатор Go API.
*/

export interface Deps {
  Config: unknown;
  DB?: unknown;
  Log?: unknown;
  Accounts?: unknown;
  Documents?: unknown;
  Limiter?: unknown;
}

//////////
// source: session.go

/**
 * SessionCookieName — имя куки сессии. Кука host-only (без Domain): браузер отдаёт её только
 * сайту, с которого она получена, то есть kupol.vladinc.ru. На api-поддомен она не уходит —
 * поэтому WebSocket и загрузки авторизуются одноразовым тикетом, а не кукой.
 */
export const SessionCookieName = "kupol_session";

//////////
// source: team.go

export interface MemberDTO {
  login: string;
  level: number /* int */;
  level_name: string;
  directorate: boolean;
  roles: RoleDTO[];
  created_at: string /* RFC 3339 */;
}
export interface CapabilityDTO {
  id: string;
  name: string;
}
export interface RoleInfoDTO {
  id: string;
  name: string;
  capabilities: CapabilityDTO[];
}

//////////
// source: team_documents.go

export interface CreateDocumentRequest extends documents.Content {
  type: string;
  code: string;
}
export interface SaveDocumentRequest {
  base_revision: number /* int */;
  content: documents.Content;
}
/**
 * PreviewRequest — тело POST /api/team/documents/:id/preview. Content — несохранённые правки из редактора;
 * без него предпросмотр строится по сохранённому.
 */
export interface PreviewRequest {
  level: number /* int */;
  content?: documents.Content;
}

//////////
// source: totp.go

